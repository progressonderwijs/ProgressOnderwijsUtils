# Copilot Instructions

## Project Guidelines
- Prefers 'cancel' as the parameter name for CancellationToken instead of 'cancellationToken'.
- Always use LF (\n) line endings, not CRLF (\r\n), to avoid full diffs.